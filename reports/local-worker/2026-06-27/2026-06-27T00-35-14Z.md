<!-- local-loop-agent -->
<!-- generated_at: 2026-06-27T00:35:14.080Z -->

# TODO: Complete Skeptic persona description in PERSONAS dict

But where? In the code, the line is:

"Skeptic": "You are 'The Skeptic'. Your job is to find reasons why this change should be REJECTED. Look"

So perhaps change it to:

"Skeptic": "You are 'The Skeptic'. Your job is to find reasons why this change should be REJECTED. Look for edge cases, bugs, or simpler ways to achieve the same result. Be a devil's advocate."  # TODO: Verify and refine persona description

But the task is to add a TODO comment. So adding a comment after the string.

Alternatively, if the string is incomplete, the correct fix is to complete it. But the instruction says to add a TODO comment. So perhaps the current code has a placeholder that needs to be completed, and adding a TODO.

But the main point is that main.py hasn't been recently touched, so it's a candidate. The current issue is that the Skeptic's description is incomplete.

So the recommended next work slice is to complete the Skeptic persona description in main.py and add a TODO comment for further refinement.

But the task should be under 20 shell lines. So the actual code change would be something like:

In agents/git-committer/main.py, update the Skeptic persona string to be complete and add a TODO comment.

But the exact task is to add a TODO comment. So perhaps:

Add a TODO comment in the Skeptic persona description in main.py to complete the missing text.

But the code might have a syntax error. However, the task is to fix it. But the instruction says to add a docstring, type hint, TODO comment, or stub function. So adding a TODO comment where the code is incomplete.

So the micro-task is: "In agents/git-committer/main.py, complete the Skeptic persona description in the PERSONAS dictionary and add a TODO comment for future refinement."

But the actual code change would be to fix the string. However, the task is to add a TODO comment. So perhaps:

# TODO: Complete Skeptic persona description with full text

But where? In the code, the line is:

"Skeptic": "You are 'The Skeptic'. Your job is to find reasons why this change should be REJECTED. Look"

So the string is cut off. So the correct fix is to complete it. But
